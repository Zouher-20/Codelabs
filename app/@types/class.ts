export interface classType {
    title: string;
    id: string;
    description?: string;
    type?: string;
}

export interface RoomType {
    title: string;
    id: string;
    description?: string;
    type?: string;
}
